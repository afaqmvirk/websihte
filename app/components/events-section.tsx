export default function EventsSection() {
  return (
    <section
      id="our-events"
      className="h-app relative w-full shrink-0 bg-black"
      aria-labelledby="our-events-heading"
    >
      <h2
        id="our-events-heading"
        className="font-arial-narrow absolute right-[max(1.25rem,4.1vw)] top-[max(2.75rem,5.5vw)] m-0 text-right text-[clamp(1.75rem,4.8vw,4.5rem)] leading-none tracking-[-0.04em] text-white lowercase"
      >
        our events
      </h2>
    </section>
  );
}
